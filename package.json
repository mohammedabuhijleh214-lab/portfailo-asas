{
  "name": "cyber-warden-portfolio",
  "private": true,
  "version": "2.0.0",
  "description": "Cyber Warden freelance portfolio",
  "scripts": {
    "build": "node scripts/build.mjs",
    "auth": "npm --prefix server start",
    "auth:install": "npm --prefix server install"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
